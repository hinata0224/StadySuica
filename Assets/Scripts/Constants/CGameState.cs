namespace Constants
{
    public enum CGameState
    {
        /// <summary> 次のボールを出す </summary>
        NextBall,
        /// <summary> ゲームオーバー </summary>
        GameOver,
        /// <summary> スコアのセーブ </summary>
        ScoreSaveEnd,
    }
}
