namespace Constants
{
    public enum CGameState
    {
        /// <summary> 次のボールを出す </summary>
        NextBall,
        /// <summary> ゲームオーバー </summary>
        GameOver,
        /// <summary> 一時停止 </summary>
        TimeStop,
    }
}
